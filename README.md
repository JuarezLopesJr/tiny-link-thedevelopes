# Tiny-Link App

A bit.ly mockup built on Meteor 1.5 using React

## Usage
Download and install Meteor:
curl https://install.meteor.com/ | sh

Inside the cloned folder run: meteor npm install.
After installation, run: meteor and go to localhost:3000
